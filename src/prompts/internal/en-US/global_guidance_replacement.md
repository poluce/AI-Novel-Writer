Correct only the globalGuidance field in the novel configuration. Write {{min_rules}}–{{max_rules}} short, stable cross-chapter rules, one per line, within {{max_chars}} characters total. Do not enumerate chapters, allocate chapter ranges, or restate the core outline. Output only the rules, with no title, explanation, Markdown, or JSON.

[Validated remaining novel configuration — context only]
{{validated_config}}
